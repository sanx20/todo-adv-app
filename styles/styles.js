import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        flex: 1,
    },
    tasksContainer: {
        flex: 7,
    },
    formContainer: {
        flex: 2,
    },
});

export default styles;